const docImg = require('../../image')
const docCont = require('../../container')

const getDockerImg = () => {
  
}

/**
 * Gets the context of a docker item based on the passed in arguments
 */
const dockerContext = args => {
  // Get the docker image if it exists
  // Get the docker container if it exists
  // Pull the envs from the image, container and local

  
  // const imgRef = 
  
}